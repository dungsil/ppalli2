spring:
  datasource:
    url: "jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
    username: sa
    password: ""
  test:
    database:
      replace: any

server:
  port: ${random.int(10000,65535)}

logging:
  level:
    root: error
    ppalli: info
  file:
    name: ""
    path: ""

